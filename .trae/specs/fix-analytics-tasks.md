# 数据分析和销售统计功能修复 - 实现计划

## 任务分解

### [ ] 任务 1：修复销售业绩排行统计问题
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改 `admin-enhanced.js` 中的 `/sales-performance` 接口
  - 修正 SQL 连接条件：`LEFT JOIN orders o ON u.id = o.user_id` → `LEFT JOIN orders o ON u.id = o.sales_user_id`
  - 新增 `avg_order_value` 字段计算，避免前端 NaN 问题
- **验收标准**：AC-1、AC-3
- **测试要求**：
  - **程序测试**：调用 `/admin/sales-performance` 接口，验证返回数据结构正确
  - **人工检查**：
    - 验证 SQL 查询逻辑是否正确
    - 检查新增代码是否遵循现有代码风格
- **备注**：注意保持与 `analysis-enhanced.js` 中类似接口的一致性

### [ ] 任务 2：修复数据分析页面 API 调用
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改 `Analytics.vue` 中的 `fetchAnalytics` 函数
  - 将 `api.analysis.getAnalytics()` 替换为 `api.analysis.getOverview()`
  - 确保数据能正确绑定到页面展示
- **验收标准**：AC-2、AC-3
- **测试要求**：
  - **人工检查**：
    - 验证 API 调用是否正确
    - 检查数据绑定是否正确
  - **程序测试**：检查 `api/index.js` 中是否存在 `getOverview` 方法
- **备注**：`getOverview` 方法在 API 配置中已存在，指向 `/analysis` 接口

### [ ] 任务 3：验证功能修复
- **优先级**：P1
- **依赖**：任务 1、任务 2
- **描述**：
  - 确保服务器正常运行
  - 使用 admin 账号登录测试各项功能
  - 对比数据库原始数据与页面显示数据
- **验收标准**：AC-1、AC-2、AC-3
- **测试要求**：
  - **人工检查**：
    - 检查销售业绩排行数据是否正确
    - 检查数据分析页面所有数据区域是否正常显示
    - 确认图表能正确渲染
- **备注**：如数据不足，可先创建测试数据

## 实现顺序
1. 任务 1：修复后端销售业绩接口
2. 任务 2：修复前端数据分析页面
3. 任务 3：整体功能验证

## 预期完成时间
- 任务 1：5 分钟
- 任务 2：2 分钟
- 任务 3：10 分钟
- 总计：~20 分钟
